# core/prompting/promptspecs/simula_utility_scorer.v1.yaml
- id: "simula-utility-scorer-v1.0"
  version: "1.0"
  scope: "simula.utility_scorer"

  identity:
    agent: "Simula.UtilityScorer"
    persona_partial: "persona_utility_scorer"

  context_lenses: []

  partials:
    - "task_framing_simula_utility_scorer"
    - "output_format_simula_utility_scorer"

  # Keep tolerant parsing—score JSON can be short and occasionally sparse
  outputs:
    parse_mode: "tolerant"
