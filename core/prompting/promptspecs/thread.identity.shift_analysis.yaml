id: thread.identity.shift_analysis
version: 1.0.0
scope: thread.identity.shift_analysis
identity:
  agent: "Thread"
  persona_partial: "identity"
safety:
  partials:
    - "safety_min"
    - "json_guard_strict"
outputs:
  schema_ref: "core/prompting/schemas/thread_identity_shift_output.json"
  parse_mode: "auto_repair"
budget_policy:
  tokens_key: "tokens"
  ms_key: "ms"
  max_tokens_fallback: 800
context_lenses:
  - "equor.identity"
partials:
  - "thread_identity_shift_analysis"
ablation_knobs:
  temperature: [0.2]
  style: ["concise"]
